import almacenes.*
import mercaderias.*
import transportes.*

test "" {
	camion.cargar(bumblebee)
	deposito.recibe(knightRider)
	deposito.recibeMultiple(camion.contenido())
	//assert.notThat(deposito.cargarCamion())
}